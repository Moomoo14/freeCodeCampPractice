// 举例
var changed = 0;

function change (num) {
  return (num + 5) / 3;
}

changed = change(10);

// 初始化变量
var processed = 0;

function process (num) {
  return (num + 3) / 5;
}

// 请把你的代码写在这条注释以下
processed = process(7);
